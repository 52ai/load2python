# /usr.bin/enc python3
# -*- coding:utf-8 -*-

# 当我们拿到一个对象的引用时，如何知道这个对象是什么类型，有哪些方法呢？

# 使用type()，来判断对象类型

# 使用isinstance(a, class)，来了解类的继承关系

# 使用dir()来获取对象的所有属性和方法

# hasattr(obj, 'x') 有属性x吗？
# setattr(obj,'y',19) 设置属性y，且初始值为19
# getattr(obj, 'y') 获取属性y的值



